# docker_selegrid
Docker images for Selenium Grid (Hub, Node)

## Build and run the docker
1. Build docker

```docker build -t selegrid .```

2. Run docker

```docker run -p 4444:4444 --name selenium-grid selegrid```
